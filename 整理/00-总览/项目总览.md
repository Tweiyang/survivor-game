# 🎮 项目总览 — 俯视角自动射击生存游戏

> 从零到可联网可部署的完整游戏，用 JS + Canvas 2D 原型实现，为 Unity 转化做准备。

---

## 🧠 全局思维导图

```mermaid
mindmap
  root((幸存者游戏))
    P0 引擎核心
      Entity-Component 架构
      生命周期管理
      场景管理器
      事件系统
      物理碰撞
      渲染管线
    P1 基础战斗
      自动射击
      怪物 AI
      伤害公式
      经验掉落
      HUD 血条
    P2 技能系统
      武器/被动 双类型
      多级升级
      三选一 UI
      数据驱动配置
    P3 多角色
      3 角色差异化
      主动技能
      选角场景
      属性条可视化
    P4 多端输入
      策略模式
      虚拟摇杆
      键位配置
      设备自动检测
    P5 联机多人
      Colyseus 服务端
      状态同步
      大厅/房间
      难度缩放
      服务端权威
    P6 关卡地图
      房间拓扑
      Boss 门机制
      瓦片地图
      多关卡切换
    P7 音频系统
      Web Audio API
      BGM 淡入淡出
      SFX 池化
      音量控制
    P8 Web 部署
      GitHub Pages
      Render 后端
      环境自适应
      优雅降级
```

---

## 📅 开发时间线

```mermaid
gantt
    title 开发阶段时间线
    dateFormat YYYY-MM-DD
    axisFormat %m/%d

    section 核心引擎
    P0 引擎核心      :done, p0, 2026-03-30, 1d

    section 游戏玩法
    P1 基础战斗      :done, p1, 2026-03-30, 1d
    P2 技能系统      :done, p2, 2026-03-30, 1d
    P3 多角色        :done, p3, 2026-03-30, 1d

    section 输入与音频
    P4 多端输入      :done, p4, 2026-03-30, 1d
    P7 音频系统      :done, p7, 2026-03-31, 1d

    section 关卡系统
    P6 关卡地图      :done, p6, 2026-03-31, 1d

    section 联机与部署
    P5 联机多人      :done, p5, 2026-03-31, 1d
    P8 Web 部署      :done, p8, 2026-04-02, 1d
    Bug 修复与优化    :done, fix, 2026-04-02, 1d
```

---

## 🏗️ 系统架构总图

```mermaid
graph TB
    subgraph 客户端["🖥️ 客户端 (Browser)"]
        Main[main.js 入口]
        GL[GameLoop 游戏循环]
        SM[SceneManager 场景管理]

        subgraph 场景["场景"]
            CS[CharacterSelectScene]
            LS[LobbyScene]
            BS[BattleScene]
        end

        subgraph 核心系统["核心系统"]
            EM[EntityManager]
            ES[EventSystem]
            IM[InputManager]
            PS[PhysicsSystem]
            RS[RenderSystem]
            AM[AudioManager]
        end

        subgraph 组件["Entity 组件"]
            HC[HealthComponent]
            SC[SpriteRenderer]
            PC[ProjectileComponent]
            SKC[SkillComponent]
            AC[AutoAttackComponent]
        end

        subgraph 网络["网络层"]
            NM[NetworkManager]
            SS[StateSynchronizer]
            Pred[PredictionSystem]
            Interp[InterpolationSystem]
        end

        subgraph UI["UI 层"]
            HUD[HUD]
            SSU[SkillSelectUI]
            GOU[GameOverUI]
            LCU[LevelCompleteUI]
        end
    end

    subgraph 服务端["🖧 服务端 (Node.js)"]
        Col[Colyseus Server]
        BR[BattleRoom]
        subgraph 服务端系统["服务端子系统"]
            SCombat[ServerCombat]
            SSpawn[ServerSpawner]
            SAI[ServerAI]
            SPhys[ServerPhysics]
            SDiff[DifficultyScaler]
        end
    end

    Main --> GL --> SM
    SM --> CS & LS & BS
    BS --> EM & ES & PS & RS
    BS --> NM
    NM -->|WebSocket| Col
    Col --> BR --> SCombat & SSpawn & SAI
    NM --> SS --> EM

    style 客户端 fill:#1a1a2e,color:#fff
    style 服务端 fill:#0f3460,color:#fff
```

---

## 📂 文件夹索引

| 文件夹 | 内容 | 核心设计技巧 |
|--------|------|-------------|
| [P0-引擎核心](./P0-引擎核心/) | ECS 架构、生命周期、场景管理 | 组件化 + Unity 映射 |
| [P1-基础战斗](./P1-基础战斗/) | 自动射击、怪物、伤害、经验 | 数据驱动 + 工厂模式 |
| [P2-技能系统](./P2-技能系统/) | 武器/被动、升级、三选一 | 策略模式 + 配置继承 |
| [P3-多角色](./P3-多角色/) | 角色差异化、主动技能 | ScriptableObject 模式 |
| [P4-多端输入](./P4-多端输入/) | 策略模式输入、虚拟摇杆 | Provider 抽象 |
| [P5-联机多人](./P5-联机多人/) | Colyseus、状态同步、大厅 | 服务端权威 + 预测/插值 |
| [P6-关卡地图](./P6-关卡地图/) | 房间拓扑、Boss 门、瓦片 | 延迟生成 + 物理隔离 |
| [P7-音频系统](./P7-音频系统/) | Web Audio API、BGM、SFX | 单例 + 节点图 |
| [P8-Web部署](./P8-Web部署/) | GitHub Pages、Render | 环境自适应 + 降级 |
| [PX-经验教训](./PX-经验教训/) | 踩坑记录、修复方案 | 调试方法论 |

---

## 🔑 核心设计原则（贯穿所有阶段）

| # | 原则 | 说明 | Unity 对应 |
|---|------|------|-----------|
| 1 | **组件化架构** | 功能拆分为独立 Component，挂载到 Entity | `MonoBehaviour` + `GameObject` |
| 2 | **数据驱动** | 所有数值在 JSON 中配置，代码不硬编码 | `ScriptableObject` |
| 3 | **事件解耦** | 系统间通过 EventSystem 通信，避免直接引用 | `UnityEvent` / `EventBus` |
| 4 | **工厂模式** | 实体创建通过 Factory，统一组件组装 | `Prefab` + `Instantiate` |
| 5 | **场景隔离** | 每个场景独立管理生命周期和资源 | `SceneManager.LoadScene` |
| 6 | **服务端权威** | 联网时所有游戏逻辑在服务端执行 | `Mirror` / `Netcode` |
| 7 | **优雅降级** | 后端不可用时保证单人可玩 | 离线模式检测 |
