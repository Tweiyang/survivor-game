# P2 — 技能系统设计

> 武器 + 被动双类型技能框架，三选一升级 UI，全数据驱动。

---

## 🧠 设计思维导图

```mermaid
mindmap
  root((P2 技能系统))
    技能框架
      SkillComponent
        武器槽位 Map
        被动槽位 Map
        可用技能查询
        升级逻辑
      技能配置
        skills.json
        weapon 类型
        passive 类型
        多级 levels 数组
    武器技能
      改变射击行为
        弹幕数量
        射速
        伤害
        散射角度
      示例
        脉冲步枪
        离子散弹枪
        等离子光束
    被动技能
      修改玩家属性
        modType percentAdd
        modType flatAdd
        stat 目标属性
      示例
        纳米护甲 +防御
        量子引擎 +移速
        生物芯片 +生命
    三选一 UI
      SkillSelectUI
        暂停游戏
        随机 3 候选
        卡片式展示
        点击选择
```

---

## 🎯 技能数据结构

```mermaid
graph TD
    subgraph skills.json
        A[skills 数组]
        A --> W1["weapon: 脉冲步枪"]
        A --> W2["weapon: 离子散弹枪"]
        A --> P1["passive: 纳米护甲"]
        A --> P2["passive: 量子引擎"]
    end

    W1 --> L1["levels: [{damage:10, count:1}, {damage:15, count:2}, ...]"]
    P1 --> L2["levels: [{value:0.1}, {value:0.2}, ...]"]
```

### 武器 vs 被动的区别

| 属性 | 武器(weapon) | 被动(passive) |
|------|-------------|--------------|
| 影响 | AutoAttackComponent 的射击参数 | 玩家 Entity 的属性值 |
| 配置 | damage, fireRate, count, spread | stat, modType, value |
| 视觉 | 改变弹幕外观/数量 | 无直接视觉变化 |
| 升级 | 每级改变多个射击参数 | 每级增加 buff 数值 |

---

## 🃏 三选一流程

```mermaid
sequenceDiagram
    participant ES as EventSystem
    participant SSU as SkillSelectUI
    participant GL as GameLoop
    participant SC as SkillComponent

    ES->>SSU: onLevelUp 事件
    SSU->>GL: pause() 暂停游戏
    SSU->>SC: getAvailableSkills(3)
    SC-->>SSU: [候选A, 候选B, 候选C]
    SSU->>SSU: 渲染卡片 UI

    Note over SSU: 玩家点击选择
    SSU->>SC: addSkill(skillId)
    SSU->>GL: resume() 恢复游戏
```

### 候选技能筛选策略

```javascript
getAvailableSkills(count) {
    // 1. 已有技能未满级 → 可升级
    // 2. 未获得的技能 → 可新增
    // 3. 随机打乱后取前 count 个
    // 4. 标记 isUpgrade / currentLevel
}
```

---

## ⚡ 设计技巧

| 技巧 | 说明 | Unity 对应 |
|------|------|-----------|
| **策略模式** | 武器/被动用同一 SkillComponent 管理，type 区分行为 | `ScriptableObject` 多态 |
| **配置继承** | levels 数组表达多级属性，代码只读当前级 | `AnimationCurve` |
| **modType** | `percentAdd` 百分比加 / `flatAdd` 固定加 | `StatModifier` 模式 |
| **随机不重复** | Fisher-Yates 洗牌保证候选不重复 | `List.Shuffle()` |
