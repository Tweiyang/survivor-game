# P3 — 多角色系统设计

> 3 个差异化角色 + 主动技能 + 选角场景，ScriptableObject 模式驱动。

---

## 🧠 设计思维导图

```mermaid
mindmap
  root((P3 多角色))
    角色差异化
      characters.json
        ranger 游侠
          高移速 高攻击
          闪避冲刺
        guardian 守护者
          高生命 高防御
          能量护盾
        engineer 工程师
          均衡属性
          超载脉冲
    选角场景
      CharacterSelectScene
        卡片式布局
        属性条可视化
        归一化对比
        选中高亮
    主动技能
      ActiveSkillComponent
        cooldown 冷却
        duration 持续
        每角色独有
      技能实现
        DashSkill 闪避冲刺
        ShieldSkill 能量护盾
        OverchargeSkill 超载脉冲
    场景流
      选角 → 大厅 → 战斗
      sceneData 传递 characterId
```

---

## 🎭 角色对比

```mermaid
graph LR
    subgraph 属性雷达图
        R["🏹 游侠<br/>HP:80 ATK:15<br/>SPD:220 DEF:2"]
        G["🛡️ 守护者<br/>HP:150 ATK:10<br/>SPD:150 DEF:8"]
        E["⚙️ 工程师<br/>HP:100 ATK:12<br/>SPD:180 DEF:5"]
    end

    R --> |"闪避冲刺"| RS["无敌0.3s + 加速"]
    G --> |"能量护盾"| GS["吸收伤害3s"]
    E --> |"超载脉冲"| ES["攻速×3 持续5s"]
```

---

## 🎮 选角 → 战斗的数据流

```mermaid
sequenceDiagram
    participant CS as CharacterSelectScene
    participant SM as SceneManager
    participant LS as LobbyScene
    participant BS as BattleScene
    participant PF as PlayerFactory

    CS->>SM: sceneData.characterId = 'ranger'
    SM->>LS: loadScene('lobby')
    Note over LS: 选择单人/多人
    LS->>SM: loadScene('battle')
    SM->>BS: init(sceneData)
    BS->>PF: create(characterId)
    PF->>PF: 读 characters.json
    PF->>PF: 组装 Components
    PF-->>BS: 玩家实体
```

---

## ⚡ 设计技巧

| 技巧 | 说明 | Unity 对应 |
|------|------|-----------|
| **ScriptableObject 模式** | `characters.json` 定义所有角色属性 | `CharacterSO.asset` |
| **属性归一化** | 属性条用 `value/maxValue` 做百分比条 | UI Slider |
| **主动技能多态** | 每个技能是独立类，共享 `ActiveSkillComponent` 接口 | 抽象类 + override |
| **场景传参** | `sceneData` 跨场景传递选角结果 | `DontDestroyOnLoad` 或 static |
