# P4 — 多端输入系统设计

> 策略模式实现 PC 键盘 + 手机触屏的统一输入抽象。

---

## 🧠 设计思维导图

```mermaid
mindmap
  root((P4 多端输入))
    策略模式
      InputManager
        统一 API
        getAxis/isPressed
        动态切换 Provider
      KeyboardInputProvider
        WASD 移动
        可配置键位
        localStorage 持久化
      TouchInputProvider
        虚拟摇杆
        技能按钮
        暂停按钮
    设备检测
      DeviceDetector
        ontouchstart 检测
        自动切换
        resize 监听
    虚拟摇杆
      VirtualJoystick
        touch 区域检测
        方向向量计算
        死区处理
        Canvas 绘制
    键位配置
      KeybindSettingsUI
        键位重映射
        冲突检测
        恢复默认
```

---

## 🎯 核心架构：策略模式

```mermaid
graph TB
    subgraph InputManager
        API["统一 API<br/>getAxis('horizontal')<br/>getAxis('vertical')<br/>isPressed('skill')"]
    end

    API --> |PC| KB["KeyboardInputProvider<br/>读取 keydown/keyup"]
    API --> |手机| TP["TouchInputProvider<br/>读取 touch 事件"]

    KB --> |输出| V1["{ x: -1~1, y: -1~1 }"]
    TP --> |输出| V2["{ x: -1~1, y: -1~1 }"]

    style InputManager fill:#2a2a4e,color:#fff
```

### 为什么用策略模式？

```javascript
// ❌ 条件分支（难维护，加新设备要改所有地方）
if (isMobile) {
    dx = touchJoystick.x;
} else {
    dx = keyboard.isDown('D') ? 1 : keyboard.isDown('A') ? -1 : 0;
}

// ✅ 策略模式（加新设备只需新增 Provider）
dx = inputManager.getAxis('horizontal'); // 自动走当前 Provider
```

**Unity 对应**：`Input System` 的 `InputAction` + `Control Scheme`

---

## 📱 虚拟摇杆实现

```mermaid
flowchart TD
    A[touchstart] --> B[记录起始点为摇杆中心]
    B --> C[touchmove]
    C --> D[计算触点到中心的偏移]
    D --> E{偏移 > 最大半径?}
    E -->|是| F[裁剪到最大半径]
    E -->|否| G[原始偏移]
    F --> H[归一化为 -1~1]
    G --> H
    H --> I[输出 axis.x / axis.y]
    J[touchend] --> K[重置为 0,0]
```

---

## ⚡ 设计技巧

| 技巧 | 说明 | Unity 对应 |
|------|------|-----------|
| **Provider 抽象** | 输入设备只需实现 `getAxis/isPressed` 接口 | `InputDevice` |
| **运行时切换** | `switchProvider()` 不重启游戏 | `Control Scheme` 切换 |
| **触屏防穿透** | `touch-action: none` CSS + `preventDefault()` | `EventSystem.IsPointerOverGameObject` |
| **键位持久化** | `localStorage` 存储自定义键位 | `PlayerPrefs` |
| **addClickOrTouch** | 统一 click + touchend，防 300ms 延迟 | `EventTrigger` |
