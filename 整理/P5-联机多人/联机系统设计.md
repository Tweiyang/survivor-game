# P5 — 联机多人系统设计

> Colyseus 服务端权威架构，状态同步，大厅/房间，难度缩放。这是最复杂的一个阶段。

---

## 🧠 设计思维导图

```mermaid
mindmap
  root((P5 联机多人))
    服务端架构
      Colyseus Server
        BattleRoom
        Schema 状态树
        生命周期 hooks
      子系统
        ServerCombat 伤害
        ServerSpawner 生成
        ServerAI 怪物AI
        ServerPhysics 碰撞
        DifficultyScaler 难度
    客户端网络层
      NetworkManager
        单例 连接管理
        WebSocket 通信
        环境自适应 URL
      StateSynchronizer
        state.onAdd/onRemove
        listen 属性变化
        实体映射 Map
      PredictionSystem
        输入预测
        服务端校正
      InterpolationSystem
        位置插值
        平滑过渡
    大厅系统
      LobbyScene
        房间列表
        创建/加入
        倒计时
        连接降级
    关键决策
      服务端权威
        伤害在服务端结算
        客户端只做视觉
      状态同步
        Schema 自动差量
        onAdd onRemove
      降级策略
        后端不可用
        单人仍可玩
```

---

## 🏛️ 服务端权威架构

```mermaid
graph TB
    subgraph 客户端A["客户端 A"]
        Input1[按键输入] --> Pred1[PredictionSystem]
        Pred1 --> Send1[send 'input']
        Recv1[接收 state] --> Interp1[InterpolationSystem]
        Interp1 --> Render1[渲染]
    end

    subgraph 服务端["服务端 (权威)"]
        Recv[接收 input] --> Physics[ServerPhysics]
        Physics --> Combat[ServerCombat]
        Combat --> State[Schema State]
        Spawner[ServerSpawner] --> State
        AI[ServerAI] --> Physics
        State --> Broadcast[广播 state 差量]
    end

    subgraph 客户端B["客户端 B"]
        Input2[按键输入] --> Pred2[PredictionSystem]
        Recv2[接收 state] --> Interp2[InterpolationSystem]
    end

    Send1 -->|WebSocket| Recv
    Broadcast -->|WebSocket| Recv1
    Broadcast -->|WebSocket| Recv2
    Pred2 --> Send2[send 'input']
    Send2 -->|WebSocket| Recv

    style 服务端 fill:#0f3460,color:#fff
```

### 为什么选择服务端权威？

| 方案 | 优点 | 缺点 |
|------|------|------|
| **P2P** | 无需服务器 | 作弊容易、同步复杂 |
| **客户端权威** | 响应快 | 不安全 |
| **服务端权威** ✅ | 防作弊、一致性 | 延迟需要预测补偿 |

---

## 📡 状态同步流程

```mermaid
sequenceDiagram
    participant C as 客户端
    participant NM as NetworkManager
    participant SS as StateSynchronizer
    participant EM as EntityManager

    Note over C,EM: 1. 连接阶段
    C->>NM: connect()
    NM->>NM: room = joinOrCreate('battle')
    NM->>SS: bind(room)

    Note over C,EM: 2. 同步阶段
    SS->>SS: state.players.onAdd → 创建远程玩家实体
    SS->>SS: state.monsters.onAdd → 创建怪物实体
    SS->>EM: add(entity)

    Note over C,EM: 3. 实时更新
    loop 每帧
        SS->>SS: playerState.listen('x') → 更新位置
        SS->>SS: monsterState.listen('hp') → 更新血量
        SS->>SS: monsterState.listen('alive') → 死亡处理
    end

    Note over C,EM: 4. 自定义消息
    NM->>SS: onMessage('damageEvent') → 闪烁 + 飘字
    NM->>SS: onMessage('levelUp') → 升级 UI
    NM->>SS: onMessage('levelComplete') → 通关 UI
```

---

## 🎮 大厅流程

```mermaid
stateDiagram-v2
    [*] --> 大厅主页
    大厅主页 --> 创建房间: 点击创建
    大厅主页 --> 房间列表: 点击加入
    大厅主页 --> 单人游戏: 点击单人

    创建房间 --> 等待室: 成功
    创建房间 --> 大厅主页: 失败(显示错误)
    房间列表 --> 等待室: 选择加入
    等待室 --> 战斗: 倒计时结束
    单人游戏 --> 战斗: 直接进入

    战斗 --> [*]: 通关/死亡
```

---

## 📊 难度缩放

```
multiplier = 1.0 + (playerCount - 1) × 0.5

1人: ×1.0    2人: ×1.5    3人: ×2.0    4人: ×2.5
```

缩放影响：怪物 HP、怪物数量、Boss HP（不影响伤害）

---

## ⚠️ 关键踩坑

| 问题 | 根因 | 修复 |
|------|------|------|
| 客户端杀了 Boss 服务端不知道 | `ProjectileComponent` 在本地造成伤害 | 联网模式 `_checkHit` 不造成伤害 |
| 怪物血条不变 | `healthComp.hp` 应为 `healthComp.currentHp` | 修正属性名 |
| 升级飞快 | 双重经验（`_awardExp` + 经验球拾取） | 移除 `_awardExp`，只保留经验球 |
| 升级公式不对 | 服务端读错了配置字段名 | `baseExp→baseExpToLevel` |
| 旧服务器没关 | 新编译代码启动失败，端口被占用 | 先 kill 旧进程 |
| 双份实体 | EventSystem 监听器泄漏（匿名函数 off 失败） | 预绑定引用 + destroy 清理 |

---

## ⚡ 设计技巧

| 技巧 | 说明 | Unity 对应 |
|------|------|-----------|
| **服务端权威** | 所有伤害/击杀/经验在服务端计算 | `Mirror` ServerRpc |
| **Schema 差量同步** | Colyseus 自动只发送变化的字段 | `NetworkVariable` |
| **客户端预测** | 输入立即本地执行，服务端校正 | `ClientNetworkTransform` |
| **延迟生成** | Boss 房间怪物等门开了才生成 | `ObjectPool` + 条件激活 |
| **消息回调清理** | `unbind()` 时 `offMessage` 所有注册的回调 | `OnDestroy` 取消订阅 |
